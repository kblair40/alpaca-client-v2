import React from "react";
import { Flex, Box, Text } from "@chakra-ui/react";

type Props = {
  label: string;
  children?: JSX.Element;
};

const DrawerSection = ({ children }: Props) => {
  return <Box>/Box>
};

export default DrawerSection;
